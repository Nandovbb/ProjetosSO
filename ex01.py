# declarar
lado: int = 0

# inicio
# receve o valor do lado de quadrado
lado = int(input("Digite o lado do quadrado: "))

# calcula a área do quadrado
area = lado*lado

# retorna o resultado da área
print(int(area))
